model Organization {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  name             String
  slug             String
  logo             String?
  metadata         String?
  isDefault        Boolean @default(false)
  stripeCustomerId String?

  members     Member[]
  invitations Invitation[]

  subscription Subscription?

  projects    Project[]
  agentTokens AgentToken[]

  gitHubInstallations GitHubInstallation[]
  linearInstallation  LinearInstallation?
  jiraInstallation    JiraInstallation?
  slackInstallation   SlackInstallation?

  @@unique([slug])
  @@map("organization")
}

model Member {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  userId         String
  user           User         @relation(fields: [userId], references: [id], onDelete: Cascade)
  role           String       @default("member")

  assignedFeedback             Feedback[]
  feedbackComments             FeedbackComment[]
  gitHubInstallations          GitHubInstallation[]
  linearInstallationsInstalled LinearInstallation[]
  jiraInstallationsInstalled   JiraInstallation[]
  slackInstallations           SlackInstallation[]

  @@index([organizationId])
  @@index([userId])
  @@map("member")
}

model Invitation {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)
  email          String
  role           String?
  status         String       @default("pending")
  expiresAt      DateTime
  inviterId      String
  user           User         @relation(fields: [inviterId], references: [id], onDelete: Cascade)

  @@index([organizationId])
  @@index([email])
  @@map("invitation")
}

model AgentToken {
  id        String   @id @default(uuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  name          String
  tokenHash     String @unique
  tokenLastFour String

  organizationId String
  organization   Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade)

  scopes     String[]
  isActive   Boolean   @default(true)
  lastUsedAt DateTime?
  revokedAt  DateTime?

  @@index([organizationId])
  @@index([tokenHash])
  @@map("agent_token")
}
